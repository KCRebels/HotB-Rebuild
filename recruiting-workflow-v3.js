(function(root,factory){
 const api=factory(root);
 if(typeof module==='object'&&module.exports)module.exports=api;
 else root.HotBRecruitingWorkflow=api;
})(typeof globalThis!=='undefined'?globalThis:this,function(root){
 'use strict';
 const DB_KEY='hotbRebuildDbV1',RECRUITING_KEY='hotbRecruitingV1';
 const ACTIVE_PLAYER='Brooklyn Gering',PROFILE_SLUG='brooklyn-gering';
 const PUBLIC_PROFILE_PATH='brooklyn-gering-recruiting-profile.html';
 const PUBLIC_PROFILE_URL='https://kcrebels.github.io/HotB-Rebuild/'+PUBLIC_PROFILE_PATH;
 const GMAIL_CLIENT_ID='412203516902-el4rhl939lb6frbbvh4krvqequ8ut7v3.apps.googleusercontent.com';
 const GMAIL_SEND_SCOPE='https://www.googleapis.com/auth/gmail.send';
 const GAMECHANGER_URL='https://web.gc.com/teams/K1E4TcPCwGKj/2027-summer-kc-rebels-16-regional-lickel';
 const DEFAULT_EVALUATION=`Brooklyn is a competitor who wants the ball and expects a lot from herself. She throws with good velocity and consistently finds the strike zone. At the plate, she makes consistent contact and has the ability to hit the ball hard. I believe there is more power coming as she continues to develop, with the potential to become a legitimate extra-base and home-run threat.\n\nBrooklyn is also an outstanding teammate and extremely coachable. She's friendly, eager to learn, actively seeks feedback, and works to apply what she's taught. She can be hard on herself because she cares about performing well, but that competitiveness and desire to improve are also two of the qualities that make her such an enjoyable player to coach.`;
 const EVENTS=[{name:'Triple Crown St. Louis Showcase',detail:'Oct. 16–18 · Chesterfield, Missouri'},{name:'Top Gun Select Invite',detail:'Oct. 30–Nov. 1 · Kansas City Metro'},{name:'RecruitLook Showcase',detail:'Nov. 6–8 · Kansas City Metro'}];
 const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
 const clean=v=>String(v??'').trim(),norm=v=>clean(v).toLowerCase().replace(/\s+/g,' ');
 function readDb(){try{return JSON.parse(localStorage.getItem(DB_KEY)||'{}')}catch{return{}}}
 function readRecruiting(){try{return JSON.parse(localStorage.getItem(RECRUITING_KEY)||'{}')||{}}catch{return{}}}
 function saveRecruiting(v){localStorage.setItem(RECRUITING_KEY,JSON.stringify(v))}
 function playerRecord(name=ACTIVE_PLAYER){return(readDb().roster||[]).find(p=>p.name===name)||null}
 function activePlayer(name){return name===ACTIVE_PLAYER}
 function buildEmailSubject(player){const positions=clean(player?.positions).replace(/\s*\|\s*/g,'/');return `${clean(player?.name)||'Player'} | ${clean(player?.grad)||'Grad Year'} | ${positions||'Positions'} | ${clean(player?.gpa)||'—'} GPA | #${clean(player?.jersey)||'—'}`}
 function shortCoachIntro(){return `Brooklyn is a competitor who wants the ball, throws with good velocity, and consistently finds the strike zone. At the plate she makes consistent contact, can hit the ball hard, and has developing extra-base power. She is an outstanding teammate, extremely coachable, and actively seeks feedback to improve.`}
 function scheduleText(){return EVENTS.map(e=>`${e.name} — ${e.detail.replace(' · ',' — ')}`).join('\n')}
 function buildBrooklynEmailBody(player,last='Coach'){return `Coach ${last||'Coach'},\n\nI wanted to introduce you to Brooklyn Gering, a 2029 player with the KC Rebels.\n\n${shortCoachIntro()}\n\nPLAYER PROFILE / CURRENT RESULTS\n${PUBLIC_PROFILE_URL}\n\nHer KC Rebels Recruiting Profile includes my coach evaluation along with her current hitting and pitching results.\n\nUPCOMING SCHEDULE\n${scheduleText()}\n\nGAMECHANGER\n${GAMECHANGER_URL}\n\nPlease feel free to contact me if you'd like additional information about Brooklyn.\n\nDan Lickel\nHead Coach | KC Rebels 16U Regional\nDirector of Recruiting | Kansas City Rebels`}
 function coachMatches(coaches,query){
  const q=norm(query);if(!q)return[];
  return [...coaches].filter(c=>norm(c.coachName).includes(q)||norm(c.collegeName).includes(q)).sort((a,b)=>{
   const aStart=norm(a.coachName).startsWith(q)||norm(a.collegeName).startsWith(q);
   const bStart=norm(b.coachName).startsWith(q)||norm(b.collegeName).startsWith(q);
   return Number(bStart)-Number(aStart)||(a.coachName||'').localeCompare(b.coachName||'');
  }).slice(0,8);
 }
 function coachLastName(coach){const parts=clean(coach?.coachName).split(/\s+/);return parts[parts.length-1]||'Coach'}
 function statsForPlayer(name){const db=readDb(),pas=(db.savedGames||[]).flatMap(g=>g.plateAppearances||[]).filter(pa=>pa.hitter===name);return root.HotBEvaluationStats?.statsForPAs?root.HotBEvaluationStats.statsForPAs(pas):{PA:pas.length,AVG:0,OBP:0,contactPct:0}}
 function bestMeasurement(db,name,type){const rows=(db.measurements||[]).filter(r=>r.player===name&&r.type===type&&Number.isFinite(Number(r.value)));if(!rows.length)return null;const timed=['Home to First','Pop Time'].includes(type),best=rows.reduce((w,r)=>!w||(timed?Number(r.value)<Number(w.value):Number(r.value)>Number(w.value))?r:w,null);return{value:Number(best.value),date:best.date||''}}
 function payload(){const db=readDb(),player=(db.roster||[]).find(p=>p.name===ACTIVE_PLAYER);if(!player)return null;const stats=statsForPlayer(ACTIVE_PLAYER),entry=readRecruiting().players?.[ACTIVE_PLAYER]||{},measurements={};['Fastball','Changeup','Exit Velocity','Home to First'].forEach(type=>{const row=bestMeasurement(db,ACTIVE_PLAYER,type);if(row)measurements[type]={value:row.value,date:row.date||''}});return{slug:PROFILE_SLUG,updatedAt:new Date().toISOString(),player:{name:player.name,jersey:player.jersey||'',grad:player.grad||'',positions:player.positions||'',gpa:player.gpa||'',school:player.school||'',interest:player.interest||'',side:player.side||'',throws:player.throws||'R',email:player.email||'',twitter:player.twitter||'',sportsRecruits:player.sportsRecruits||'',photo:player.photo||''},coachEvaluation:entry.coachEvaluation||DEFAULT_EVALUATION,coachEvaluationUpdatedAt:entry.coachEvaluationUpdatedAt||'',hitting:{PA:stats.PA,AVG:Number(stats.AVG||0).toFixed(3).replace(/^0/,''),OBP:Number(stats.OBP||0).toFixed(3).replace(/^0/,''),CONTACT:`${Math.round((stats.contactPct||0)*100)}%`},pitching:{IP:player.pitcherIP||'—',ERA:player.pitcherERA||'—',WHIP:player.pitcherWHIP||'—','K/BB':player.pitcherKBB||'—',OBA:player.pitcherOBA||'—','STRIKE %':player.pitcherStrikePct||'—'},measurements,events:EVENTS,gameChangerUrl:GAMECHANGER_URL,sportsRecruitsUrl:player.sportsRecruits||'https://my.sportsrecruits.com/athlete/brooklyn_gering'}}
 function evaluationStatus(){const stamp=readRecruiting().players?.[ACTIVE_PLAYER]?.coachEvaluationUpdatedAt;if(!stamp)return'Current';const d=new Date(stamp);return Number.isNaN(d.getTime())?'Current':`Current · Updated ${d.toLocaleDateString('en-US',{month:'short',year:'numeric'})}`}
 function shell(inner){return `<div class="rw-backdrop" data-rw-close><section class="rw-modal" role="dialog" aria-modal="true">${inner}</section></div>`}
 function closeModal(){document.querySelector('.rw-backdrop')?.remove()}
 async function publishProfile(force=false){if(!root.firebase?.apps?.length)return false;const user=root.firebase.auth().currentUser;if(!user||user.isAnonymous||String(user.email||'').toLowerCase()!=='hotbkcrebels@gmail.com')return false;const data=payload();if(!data)return false;const serialized=JSON.stringify(data);if(!force&&sessionStorage.getItem('hotbRecruitingPublishedV1')===serialized)return true;try{await root.firebase.firestore().collection('recruitingProfiles').doc(PROFILE_SLUG).set(data,{merge:true});sessionStorage.setItem('hotbRecruitingPublishedV1',serialized);return true}catch(error){console.warn('Recruiting Profile publish is waiting for Firestore rules deployment.',error);return false}}
 function openEvaluation(){const entry=readRecruiting().players?.[ACTIVE_PLAYER]||{};document.body.insertAdjacentHTML('beforeend',shell(`<div class="rw-modal-head"><div><span>COACH EVALUATION</span><h2>Brooklyn Gering</h2></div><button class="rw-close" data-rw-close>Close</button></div><p class="rw-help">This is an occasional edit. Changes here also update Brooklyn's Recruiting Profile.</p><textarea id="rwCoachEvaluation">${esc(entry.coachEvaluation||DEFAULT_EVALUATION)}</textarea><div class="rw-actions"><button class="rw-secondary" data-rw-close>Cancel</button><button class="rw-primary" id="rwSaveEvaluation">Save Evaluation</button></div>`));document.querySelector('#rwSaveEvaluation')?.addEventListener('click',async()=>{const value=clean(document.querySelector('#rwCoachEvaluation')?.value);if(!value)return;const store=readRecruiting();store.players=store.players||{};store.players[ACTIVE_PLAYER]={...(store.players[ACTIVE_PLAYER]||{}),coachEvaluation:value,coachEvaluationUpdatedAt:new Date().toISOString()};saveRecruiting(store);closeModal();await publishProfile(true);const text=document.querySelector('.rw-eval-status');if(text)text.textContent=evaluationStatus()})}
 function requestGmailAccessToken(){return new Promise((resolve,reject)=>{if(!root.google?.accounts?.oauth2){reject(new Error('Google sign-in is still loading. Wait a few seconds and try again.'));return}const client=root.google.accounts.oauth2.initTokenClient({client_id:GMAIL_CLIENT_ID,scope:GMAIL_SEND_SCOPE,callback:r=>r.error?reject(new Error('Gmail authorization was not completed.')):resolve(r.access_token),error_callback:()=>reject(new Error('Gmail authorization was closed or blocked.'))});client.requestAccessToken({prompt:'select_account consent'})})}
 function utf8Base64(value){const bytes=new TextEncoder().encode(String(value));let binary='';for(let i=0;i<bytes.length;i+=0x8000)binary+=String.fromCharCode(...bytes.subarray(i,i+0x8000));return btoa(binary)}
 function htmlText(v){return esc(v).replace(/(https?:\/\/[^\s<]+)/g,url=>`<a href="${url}" style="color:#b3262d">${url}</a>`)}
 function bodyHtml(body){let html='';String(body||'').split(/\r?\n/).forEach(line=>{const v=line.trim();if(!v){html+='<div style="height:10px"></div>';return}if(['PLAYER PROFILE / CURRENT RESULTS','UPCOMING SCHEDULE','GAMECHANGER'].includes(v)){html+=`<h3 style="margin:20px 0 8px;font-size:16px">${esc(v)}</h3>`;return}html+=`<div style="margin:3px 0">${htmlText(v)}</div>`});return `<div style="font-family:Arial,sans-serif;font-size:15px;line-height:1.5;color:#111">${html}</div>`}
 async function sendEmail(token,to,cc,subject,body){const headers=[`To: ${to}`,cc?`Cc: ${cc}`:'',`Subject: =?UTF-8?B?${utf8Base64(subject)}?=`,'MIME-Version: 1.0','Content-Type: text/html; charset="UTF-8"','Content-Transfer-Encoding: 8bit'].filter(Boolean).join('\r\n'),raw=utf8Base64(`${headers}\r\n\r\n${bodyHtml(body)}`).replace(/\+/g,'-').replace(/\//g,'_').replace(/=+$/,'');const response=await fetch('https://gmail.googleapis.com/gmail/v1/users/me/messages/send',{method:'POST',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify({raw})});if(!response.ok){const detail=await response.json().catch(()=>({}));throw new Error(detail?.error?.message||'Gmail rejected the message. Nothing was sent.')}}
 function openEmail(){
  const db=readDb(),player=(db.roster||[]).find(p=>p.name===ACTIVE_PLAYER);if(!player)return;
  const coaches=db.coaches||[];let selected=null,searchTimer=null;
  document.body.insertAdjacentHTML('beforeend',shell(`<div class="rw-modal-head"><div><span>EMAIL COACH</span><h2>Brooklyn Gering</h2></div><button class="rw-close" data-rw-close>Close</button></div><p class="rw-help">Start typing a coach name or college. Tap a match to select that coach.</p><label class="rw-label rw-coach-search">Coach or College<input id="rwCoachSearch" autocomplete="off" inputmode="search" placeholder="Start typing a name or college"><div class="rw-search-results" id="rwCoachMatches" hidden></div></label><div class="rw-email-meta"><b>To:</b> <span id="rwTo">Select a coach</span><br><b>CC:</b> <span>${esc(player.email||'None')}</span><br><b>Subject:</b> <span>${esc(buildEmailSubject(player))}</span></div><label class="rw-label">Email Message — You Can Edit It Here<textarea id="rwEmailBody">${esc(buildBrooklynEmailBody(player,'Coach'))}</textarea></label><p class="rw-help">Nothing sends until you confirm and Gmail completes authorization.</p><div class="rw-actions"><button class="rw-secondary" data-rw-close>Cancel</button><button class="rw-primary" id="rwSendEmail" disabled>Send with Gmail</button></div>`));
  const input=document.querySelector('#rwCoachSearch'),results=document.querySelector('#rwCoachMatches'),to=document.querySelector('#rwTo'),textarea=document.querySelector('#rwEmailBody'),send=document.querySelector('#rwSendEmail');
  const choose=coach=>{selected=coach;input.value=`${coach.coachName||''}${coach.collegeName?` — ${coach.collegeName}`:''}`;to.textContent=coach.coachEmail||'No saved email';textarea.value=buildBrooklynEmailBody(player,coachLastName(coach));send.disabled=!clean(coach.coachEmail);results.hidden=true};
  const renderMatches=()=>{const matches=coachMatches(coaches,input.value);results.replaceChildren();for(const coach of matches){const button=document.createElement('button');button.type='button';button.className='rw-search-result';const primary=document.createElement('b'),secondary=document.createElement('span');primary.textContent=coach.coachName||'Coach';secondary.textContent=`${coach.collegeName||''}${coach.coachEmail?` · ${coach.coachEmail}`:''}`;button.append(primary,secondary);button.addEventListener('pointerdown',event=>{event.preventDefault();choose(coach)});results.append(button)}results.hidden=!matches.length};
  input.addEventListener('input',()=>{selected=null;send.disabled=true;to.textContent='Select a coach';clearTimeout(searchTimer);searchTimer=setTimeout(renderMatches,60)});
  input.addEventListener('focus',renderMatches);
  input.addEventListener('blur',()=>setTimeout(()=>{results.hidden=true},180));
  send.addEventListener('click',async event=>{const email=clean(selected?.coachEmail);if(!email){alert('Select a saved coach with an email address first.');return}if(!confirm(`Send this recruiting email now to ${email}${player.email?` and CC ${player.email}`:''}?`))return;const button=event.currentTarget;button.disabled=true;button.textContent='Connecting to Gmail…';try{const token=await requestGmailAccessToken();button.textContent='Sending…';await sendEmail(token,email,player.email||'',buildEmailSubject(player),textarea.value||'');closeModal();alert('Recruiting email sent through Gmail.')}catch(error){button.disabled=false;button.textContent='Send with Gmail';alert(error?.message||'Gmail could not send this email. Nothing was sent.')}});
 }
 function inject(){const evalApp=document.querySelector('.eval-app'),select=document.querySelector('#evalSelect');if(!evalApp||!select)return;const name=select.value,existing=document.querySelector('#rwRecruitingSection');if(existing&&existing.dataset.player===name)return;if(existing)existing.remove();const player=name==='Team'?null:playerRecord(name);if(!player)return;const enabled=activePlayer(name),anchor=document.querySelector('.player-card.player-profile');if(!anchor)return;const evalLine=enabled?`<div class="rw-eval-line"><span><b>Coach Evaluation:</b> <span class="rw-eval-status">${esc(evaluationStatus())}</span></span><button type="button" class="rw-evaluation-link">Edit</button></div>`:`<div class="rw-eval-line disabled"><span><b>Coach Evaluation:</b> Coming Soon</span></div>`;anchor.insertAdjacentHTML('afterend',`<section class="rw-recruiting" id="rwRecruitingSection" data-player="${esc(name)}"><div class="rw-section-head"><div><span>RECRUITING</span><h2>Recruiting Workflow</h2></div>${enabled?'<small>Brooklyn pilot</small>':'<small>Coming Soon</small>'}</div><div class="rw-buttons"><button type="button" class="rw-profile" ${enabled?'':'disabled'}>Recruiting Profile</button><button type="button" class="rw-email" ${enabled?'':'disabled'}>Email Coach</button></div>${evalLine}${enabled?'<p>Public scouting report · Coach-to-coach introduction</p>':'<p>Recruiting controls are visible for planning but inactive for this player.</p>'}</section>`);if(enabled){document.querySelector('.rw-profile')?.addEventListener('click',()=>root.open(PUBLIC_PROFILE_PATH,'_blank','noopener'));document.querySelector('.rw-email')?.addEventListener('click',openEmail);document.querySelector('.rw-evaluation-link')?.addEventListener('click',openEvaluation);publishProfile(false)}}
 function init(){if(typeof document==='undefined')return;document.addEventListener('click',event=>{if(event.target.matches('[data-rw-close]'))closeModal()});const observer=new MutationObserver(()=>requestAnimationFrame(inject));observer.observe(document.getElementById('app')||document.body,{childList:true,subtree:true});document.addEventListener('change',event=>{if(event.target?.id==='evalSelect')setTimeout(inject,0)});if(root.firebase?.auth)root.firebase.auth().onAuthStateChanged(()=>setTimeout(()=>publishProfile(false),200));inject()}
 if(typeof document!=='undefined'&&document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
 return{ACTIVE_PLAYER,PUBLIC_PROFILE_URL,DEFAULT_EVALUATION,EVENTS,activePlayer,buildEmailSubject,buildBrooklynEmailBody,shortCoachIntro,coachMatches,payload};
});